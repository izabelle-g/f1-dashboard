import SeasonList from "./SeasonList";

const Header = (props) => {
    // TODO: handler for buttons
    // TODO: handler for seasons
/*
    return(
        <div>
            <h2>F1 Dashboard</h2>

            <label for="seasons">Season</label>
            <select name="seasons">
                { props.data.map( s => <SeasonList data={s}/>) }
            </select>

            <button>Favourites</button>
            <button>About</button>
        </div>
    )*/
};

export default Header